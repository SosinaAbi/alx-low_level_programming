#include <stdlib.h>
#include <stdio.h>

/**
 * main - Prints program name.
 * @argc: The size of the argv array
 * @argv: String array of size argc
 *
 * Return: 0 is successful.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
